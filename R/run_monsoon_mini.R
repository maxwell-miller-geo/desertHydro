# Run monsoon
# require(desertHydro)
# #
# date <- "2022-07-24"
# time <- NaN
# dem <- "dem-test.tif"
# rain_method <- "gauges"
# overwrite <- T
# courant <- 0.8
# folderName <- paste0(date,"-", stringr::str_split(dem, "\\.")[[1]][1], "-", rain_method, "-c-", courant)
# desertHydro:::monsoon(folderName,
#                       time = time,
#                       date = date,
#                       rain_method = rain_method,
#                       dem = dem,
#                       overwrite = overwrite,
#                       courant = courant)
# print(paste0("Finished model:", folderName))
# date <- "2021-07-22"
# folderName <- paste0(date,"-", stringr::str_split(dem, "\\.")[[1]][1], "-", rain_method, "-c-", courant)
# desertHydro:::monsoon(folderName,
#                       time = time,
#                       date = date,
#                       rain_method = rain_method,
#                       dem = dem,
#                       overwrite = overwrite,
#                       courant = courant)
# print(paste0("Finished model:", folderName))
# date <- "2022-07-05"
# folderName <- paste0(date,"-", stringr::str_split(dem, "\\.")[[1]][1], "-", rain_method, "-c-", courant)
# desertHydro:::monsoon(folderName,
#                       time = time,
#                       date = date,
#                       rain_method = rain_method,
#                       dem = dem,
#                       overwrite = overwrite,
#                       courant = courant)
# print(paste0("Finished model:", folderName))
# date <- "2022-07-15"
# folderName <- paste0(date,"-", stringr::str_split(dem, "\\.")[[1]][1], "-", rain_method, "-c-", courant)
# desertHydro:::monsoon(folderName,
#                       time = time,
#                       date = date,
#                       rain_method = rain_method,
#                       dem = dem,
#                       overwrite = overwrite,
#                       courant = courant)
# print(paste0("Finished model:", folderName))
# print("Finished all models!")
# # print("Finished!")
