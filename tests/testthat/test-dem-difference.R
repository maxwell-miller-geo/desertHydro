test_that("DEM difference", {
  # dem <- terra::rast(file.path(model()@watershedPath, "dem.tif"))
  # # Load in full soil stack
  # SoilStack <- terra::rast(file.path(model()@watershedPath, "full-soil-stack.tif"))
  # mod_dem <- SoilStack$model_dem
  # elevation_adjustment <- rast_diff(dem, mod_dem)
  #
  # terra::plot(elevation_adjustment != 0)
})
